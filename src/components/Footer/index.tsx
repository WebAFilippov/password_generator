import s from "./Footer.module.scss";

const Footer = () => {
  return (
    <footer className={s.footer}>
      <div>Footer</div>
      <div>Footer</div>
      <div>Footer</div>
    </footer>
  );
};

export default Footer;
